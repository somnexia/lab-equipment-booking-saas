extends layout

block content
  h1.page-title Admin
  p.page-lead User and equipment category management — coming in a future iteration.

  .card(style="max-width:520px")
    p
      | APIs available for now:
      code GET /api/users
      | ,
      code GET /api/equipment-categories
      | .
    p
      a(href="/dashboard") ← Back to catalog
