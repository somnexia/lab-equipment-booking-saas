extends layout

block content
  .card.auth-card
    h1 Дополнительный пароль
    p.page-lead Один раз после входа — до выхода повторно не спрашиваем.

    form#simpleForm
      .form-row
        label(for="password") Пароль
        input#password(type="password", name="password", placeholder="SIMPLE_PASSWORD из .env", required)

      button.btn.btn-primary(type="submit") Продолжить

block scripts
  script.
    document.getElementById('simpleForm').addEventListener('submit', async function (e) {
      e.preventDefault();

      const res = await fetch('/api/auth/simple-password', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ password: document.getElementById('password').value }),
        credentials: 'include'
      });

      if (res.ok) {
        window.location.href = '/dashboard';
      } else {
        alert('Неверный пароль');
      }
    });
